export interface Photo {
    id: string
    uid: string
    fileName: string
    url: string
}

export interface PhotoViewModel {
    id: string
    uid: string
    fileName: string
    userName: string
    url: string
}